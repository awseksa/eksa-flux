# This is the configuration file for the etcd server.

# Human-readable name for this member.

# Path to the data directory.

# Path to the dedicated wal directory.
#wal-dir: /data/wal

# Number of committed transactions to trigger a snapshot to disk.
snapshot-count: 10000

# Time (in milliseconds) of a heartbeat interval.
heartbeat-interval: 100

# Time (in milliseconds) for an election to timeout.
election-timeout: 1000

# Raise alarms when backend size exceeds the given quota. 0 means use the
# default quota.
quota-backend-bytes: {{ sub ( mul (trimSuffix "Gi" .Values.volume.storageCapacity ) 1073741824 ) ( div (mul ( mul (trimSuffix "Gi" .Values.volume.storageCapacity ) 1073741824 ) 20 ) 100 ) }}
# List of comma separated URLs to listen on for peer traffic.

# Maximum bytes permitted for a etcd transaction value
max-request-bytes: 15728640

# List of comma separated URLs to listen on for client traffic.

# Maximum number of snapshot files to retain (0 is unlimited).
max-snapshots: 5

# Maximum number of wal files to retain (0 is unlimited).
max-wals: 5

# Comma-separated white list of origins for CORS (cross-origin resource sharing).
cors:

# List of this member's peer URLs to advertise to the rest of the cluster.
# The URLs needed to be a comma-separated list.

# List of this member's client URLs to advertise to the public.
# The URLs needed to be a comma-separated list.

# Discovery URL used to bootstrap the cluster.
discovery:

# Valid values include 'exit', 'proxy'
discovery-fallback: 'proxy'

# HTTP proxy to use for traffic to discovery service.
discovery-proxy:

# DNS domain used to bootstrap initial cluster.

# Initial cluster configuration for bootstrapping.

# Initial cluster token for the etcd cluster during bootstrap.

# Initial cluster state ('new' or 'existing').

# Reject reconfiguration requests that would cause quorum loss.
strict-reconfig-check: false

# Accept etcd V2 client requests
enable-v2: true

# Enable runtime profiling data via HTTP server
enable-pprof: true

# Valid values include 'on', 'readonly', 'off'
proxy: 'off'

# Time (in milliseconds) an endpoint will be held in a failed state.
proxy-failure-wait: 2000

# Time (in milliseconds) of the endpoints refresh interval.
proxy-refresh-interval: 30000

# Time (in milliseconds) for a dial to timeout.
proxy-dial-timeout: 1000

# Time (in milliseconds) for a write to timeout.
proxy-write-timeout: 5000

# Time (in milliseconds) for a read to timeout.
proxy-read-timeout: 0

client-transport-security:
  # Path to the client server TLS cert file.
  cert-file:

  # Path to the client server TLS key file.
  key-file:

  # Enable client cert authentication.
  client-cert-auth: false

  # Path to the client server TLS trusted CA cert file.
  trusted-ca-file:

  # Client TLS using generated certificates
  auto-tls: false

peer-transport-security:
  # Path to the peer server TLS cert file.
  cert-file:

  # Path to the peer server TLS key file.
  key-file:

  # Enable peer client cert authentication.
  client-cert-auth: false

  # Path to the peer server TLS trusted CA cert file.
  trusted-ca-file:

  # Peer TLS using generated certificates.
  auto-tls: false

# Enable debug-level logging for etcd.
enable-grpc-gateway: true

logger: zap

# Specify 'stdout' or 'stderr' to skip journald logging even when running under systemd.
#log-outputs: [stderr]

# Force to create a new one member cluster.
force-new-cluster: false

auto-compaction-mode: periodic
auto-compaction-retention: "5"
data-dir: "/data/__ETCD_NAME"
log-level: "info"
name: "__ETCD_NAME"
initial-cluster-token: "etcd-cluster-1"
initial-cluster-state: "new"

#initial-cluster: ""
#discovery-srv: "etcd"
#initial-advertise-peer-urls: "http://__ETCD_NAME.etcd.__ETCD_NAMESPACE.svc.cluster.local:2380"
#advertise-client-urls: "http://__ETCD_NAME.etcd.__ETCD_NAMESPACE.svc.cluster.local:2379"
#listen-client-urls: "http://__ETCD_NAME.etcd.__ETCD_NAMESPACE.svc.cluster.local:2379,http://localhost:2379"
#listen-peer-urls: "http://__ETCD_NAME.etcd.__ETCD_NAMESPACE.svc.cluster.local:2380"

initial-cluster: "__ETCD_INITIAL_CLUSTER"
discovery-srv: ""
initial-advertise-peer-urls: "http://__ETCD_NAME.etcd.__ETCD_NAMESPACE.svc.__DOMAIN:2380"
advertise-client-urls: "http://__ETCD_NAME.etcd.__ETCD_NAMESPACE.svc.__DOMAIN:2379"
listen-client-urls: "http://0.0.0.0:2379"
listen-peer-urls: "http://0.0.0.0:2380"



